interface CardProps {
  projectImg: string,
  title:string,
  about: string,
  github: string,
  website: string,
}

const Card = ({  } : CardProps) => {
  return(
    <div className="max-w-sm rounded overflow-hidden shadow-lg">
      <div>

      </div>
    </div>
  );
};

export default Card;